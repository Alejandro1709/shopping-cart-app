import styled from 'styled-components';

export const Input = styled.input`
  padding: 12px 16px;
  border: 1px solid var(--border-clr);
  font-size: 1.6rem;
  font-weight: 400;
  border-radius: 4px;
  width: 100%;
`;
